import 'package:flutter/material.dart';

class InputsScreens extends StatelessWidget {
  const InputsScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Inputs Screens'),
    );
  }
}
